---
name: draft-report
description: Auto-draft a complete, grounded GitHub issue or PR report for RainyinSaiGon/ember, then write it to `.claude/drafts/draft-issue.md`. No issue templates exist yet — uses a standard fallback structure grounded in README.md and the ember spec.
---

> Generated by `specialize-skill` from the general `draft-report` skill for
> `RainyinSaiGon/ember`. Concrete to THIS repo. Re-run `specialize-skill draft-report`
> after the general skill changes.

# Draft Report — RainyinSaiGon/ember

**Platform:** GitHub · **Repo:** `RainyinSaiGon/ember`

## Baked-in values

**Issue templates** (all exist — use verbatim as skeleton):

| Tag in title | Template path |
|---|---|
| `[Bug]` | `.github/ISSUE_TEMPLATE/bug_report.md` |
| `[Task]` / `[Feature]` | `.github/ISSUE_TEMPLATE/implementation-task.md` |
| `[Chore]` / `[Docs]` / refactor | `.github/ISSUE_TEMPLATE/chore.md` |
| PR / describing a merged change | `.github/pull_request_template.md` |

Strip YAML frontmatter from the chosen template; keep `title:` as the suggested title prefix.

**Grounding sources** (canonical; verify they exist before citing):
- `README.md` — architecture overview, feature track table, design decisions
- `docs/spec.md` — full build plan, track-by-track acceptance criteria, design rationale, interview talking points

**Actor vocabulary:** Solo developer project. For functional issues say "developer/user of the server".
For infra/refactor/track work say "Developer / maintainer (no end-user-facing change)".

**Title conventions:** No established prefix format yet (early project).
Use the `[Tag] Short description` format as a sensible default:
- `[Feature] Add RESP parser state machine`
- `[Bug] Fix partial-read handling in RESP parser`
- `[Chore] Set up Gradle run task`
- `[Docs] Document NIO event loop design`

**Label inference from tag:**
- `[Bug]` → `bug`
- `[Feature]` → `enhancement`
- `[Docs]` → `documentation`
- `[Task]` / `[Chore]` → _(no label)_

---

## Inputs

- A task title and/or short description (from `$ARGUMENTS` or the conversation).
- Optionally the user specifies "issue" or "PR".
- Output path defaults to `.claude/drafts/draft-issue.md`.

## Step 1 — Pick the template

Read the matching template file from the table above (strip its YAML frontmatter; keep `title:` as the suggested title prefix). Use it verbatim as the skeleton — fill every `<!-- ... -->` section with real content.

For issues with no clear tag match, default to `implementation-task.md`.
For PR reports, use `.github/pull_request_template.md`.

## Step 2 — Gather grounding (do not fabricate)

1. Read `README.md` for architecture and feature track context.
2. Read `docs/spec.md` for the detailed track acceptance criteria and design rationale. Reference specific track names (Track 0, Track A, etc.) when relevant.
3. Search the codebase for keywords from the task title to identify affected files.
4. For PR reports: `git diff main...HEAD --stat` and read changed file content.
5. Reconcile: if the spec and code disagree, the code wins. Note discrepancies explicitly.

## Step 3 — Fill every section

- **Summary** — 2–5 sentences, plain language. State the problem and change.
- **Context** — reference the relevant track from the spec (e.g. "This is part of Track 0 — Foundation"). Cite `docs/spec.md` section if directly relevant.
- **Actor** — use the vocabulary above.
- **Proposed Behavior** — numbered concrete steps; for a new feature, describe the end state; for a refactor, describe the before/after.
- **Acceptance Criteria** — drawn from the track's Acceptance section in the spec where applicable. Add task-specific ones.
- **Out of Scope** — name the next-track work this shouldn't absorb.
- **Dependencies** — real blockers only; "None known" if none.

## Step 4 — Write the draft file

Write to `.claude/drafts/draft-issue.md`. Create the directory if needed.

```markdown
---
title: "[Tag] <short title>"
kind: issue            # issue | pr
template: fallback
labels: [<inferred labels>]
---

<fully-filled body — no placeholders>
```

## Step 5 — Deliver

1. Confirm the file path written.
2. Print the filled report in a fenced ```markdown block for inline review.
3. Offer follow-ups (don't run without asking):
   - Run `create-issue` to open it on GitHub.
   - Or save to `docs/tasks/<slug>.md`.

## Quality bar

- Draft written to `.claude/drafts/draft-issue.md` with valid frontmatter.
- Zero `<!-- ... -->` comments and zero dangling `docs/...` placeholders in the body.
- Every spec reference is to a real section that exists in `docs/spec.md`.
- Checkboxes reflect a real decision.
- Specific enough that a teammate could start work from it alone.
