---
name: ship
description: Finish a working branch for RainyinSaiGon/ember end-to-end — sync with main, build, test, code-review, security-review, commit, open-pr, watch CI. Stops at the first failed gate. No CI workflows configured yet; local gates are build + test.
---

> Generated by `specialize-skill` from the general `ship` skill for
> `RainyinSaiGon/ember`. Concrete to THIS repo. Re-run `specialize-skill ship`
> after the general skill changes.

# Ship — RainyinSaiGon/ember

**Platform:** GitHub · **Repo:** `RainyinSaiGon/ember` · **Base branch:** `main`

## Baked-in values

**CI:** No `.github/workflows/` configured yet. Local gates are the source of truth until CI is added.

**Lint:** None configured — skip lint gate. (Add this entry when a linter is set up.)

**Build command (all Java):**
```bash
./gradlew build
```

**Test command (all Java):**
```bash
./gradlew test
```

**Changed-area map:** Single area — all source is Java under `src/`. Run build + test for every change.

**Commit convention:** Sentence-case prose.
Example: `Add incremental RESP parser` or `Fix partial-read handling in RESP parser`.
No conventional-commit prefix required.

**Review skills:** `code-review`, `security-review`

**CI watch command:**
```bash
gh pr checks --watch
```

---

## Rules of engagement

- **Gated & sequential:** stop at the first failure, show output, let the user decide. Never silently continue past a red gate.
- **Confirm outward actions:** commit and PR require explicit go-ahead.
- **Read-only stages run freely:** build/test/reviews need no confirmation.
- **Optional skips:** honor `--skip-tests`, `--skip-security`, `--draft`, `--no-pr`, `--no-watch`. Announce any skip in the final report.
- **Final report:** per-stage status table (✅ / ❌ / ⏭️ skipped).

## Step 0 — Preflight + sync with main

```bash
git rev-parse --abbrev-ref HEAD   # must not be main
git fetch origin
git merge origin/main             # stop on conflict; list conflicted files
git diff main...HEAD --stat       # confirm there are changes
```

If the branch is `main`, stop. On merge conflict, stop and list `git diff --name-only --diff-filter=U`. If there are no changes vs. main, stop — nothing to ship.

## Step 1 — Lint (skip — not configured)

No linter configured. ⏭️ skipped. Note this in the final report.

## Step 2 — Build (gate)

```bash
./gradlew build
```

On failure: show the compiler/Gradle errors and stop.

## Step 3 — Test (gate)

```bash
./gradlew test
```

On failure: show the failing test output and stop. Skippable with `--skip-tests`.

## Step 4 — Code review (gate)

Invoke the **`code-review`** skill on the current diff. Summarize findings. If high-confidence correctness bugs surface, ask whether to fix before continuing.

## Step 5 — Security review (gate)

Invoke the **`security-review`** skill on the pending changes. Treat high-severity findings as blocking unless the user overrides. Skippable with `--skip-security`.

## Step 6 — Commit (confirm)

- Stage intended changes explicitly (don't blindly `git add -A` if unrelated files are dirty; ask if unsure).
- Compose a sentence-case prose commit message. Show the message + staged file list, get a go-ahead, then commit.
- Commit on the feature branch only.

## Step 7 — Open PR (confirm)

Invoke this repo's **`open-pr`** skill (concrete version at `.claude/skills/open-pr/SKILL.md`). Stop here if `--no-pr` was passed.

## Step 8 — Watch CI

```bash
gh pr checks --watch
```

Report pass/fail. If CI doesn't exist yet, note "No CI configured — skipping watch." Skippable with `--no-watch`.

## Step 9 — Report

Print the per-stage status table, the commit hash, the PR URL, and CI status. Note any skipped stages and remind the user to add reviewers.

| Stage | Status |
|---|---|
| Sync with main | ✅ / ❌ |
| Lint | ⏭️ not configured |
| Build (`./gradlew build`) | ✅ / ❌ |
| Test (`./gradlew test`) | ✅ / ❌ / ⏭️ |
| Code review | ✅ / ❌ |
| Security review | ✅ / ❌ / ⏭️ |
| Commit | ✅ / ❌ |
| Open PR | ✅ / ❌ / ⏭️ |
| Watch CI | ✅ / ❌ / ⏭️ |
